glog -n8 widgets-rewrite

git checkout widget-talk-begin

make a widget app
make sure it's not a shadow build

add to .pro:
+ SOURCES +=
        ../qtquick/sortfilterprocessmodel.cpp \
        ../qtquick/processmodel.cpp \
        ../src/proc.cpp \
        ../src/ttystr.cpp \
        ../src/uidstr.cpp \

+ HEADERS +=
        ../qtquick/sortfilterprocessmodel.h \
        ../qtquick/processmodel.h \

make it work.

Next steps:

git next-show widget-talk-begin widgets-rewrite

82e3cec That was easy
1c37836 Enable sorting; resize columns to contents; quit action
  add sort field property
  add quit action with icon from theme: gtk-quit ; add to toolbar and menu
  let Creator create a slot
> e33bbd3 prep: Add update interval spinbox and resize columns button to the toolbar; icons  <-- checkout
  add HBox toolbarStuff
  add labels and spinbox
  wire spinbox to ground: setUpdateInterval(int)
  don't forget timerEvent impl
  add resize button to toolbarStuff, wire to resizeColumnsToContents
> feb7179 Wire up the spinbox; add filter feature; roles for type and numeric value          <-- checkout
  wire textChanged(QString) to ground: setFilter(QString)
> a2ede0a Add a custom delegate for percentage columns									   <-- checkout
695a607 add a "kill process" menu item on context menu
> 0472599 Use QScroller to enable flicking the table								    	   <-- checkout
